/**
 * ************************************
 *
 * @module  PreviousDrawingThumbnail
 * @date
 * @description presentation component that renders a drawing and allows user to draw
 *
 * ************************************
 */

import React from 'react';

const Thumbnail = (props) => {
  return <div className="thumbnail"></div>;
};

export default Thumbnail;
